h1 Pool Party!

p Welcome back
= current_user.email

- unless current_user.itineraries.empty?
  P Your events
  ul
    - current_user.itineraries.each do |item|
      li
        = link_to item.title, "itineraries/#{item.id}/edit"
- else
  p
    | No events found.  Please add some!
      Thank you!

= link_to 'Create', '/itineraries/new', class: 'btn btn-green button'

